============
Installation
============

At the command line::

    $ easy_install bankloader

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv bankloader
    $ pip install bankloader
